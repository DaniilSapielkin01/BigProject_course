import React, { Component } from "react";
import stl from "./Header.module.sass";

export const Header = () => {
  return (
    <header className={stl.header}>
      <p>Header</p>
    </header>
  );
};
